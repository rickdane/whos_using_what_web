<%= form_for(@anewtable) do |f| %>
  <% if @anewtable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anewtable.errors.count, "error") %> prohibited this anewtable from being saved:</h2>

      <ul>
      <% @anewtable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tablevalue %><br />
    <%= f.text_field :tablevalue %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
